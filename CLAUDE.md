# Repository conventions

## Commit and pull request attribution

Do **not** add a `Claude-Session:` trailer to commit messages, and do not put a
`https://claude.ai/code/session_...` URL in pull request descriptions, issue
comments, or review comments.

This repository is public, so anything in a commit message or a PR body is
world-readable. That session URL is not a share link — fetched without
credentials it returns an authentication wall, not a transcript — but it is
still an identifier this project has no reason to publish.

Keep the `Co-Authored-By: Claude <noreply@anthropic.com>` trailer. Authorship
attribution is wanted; the session pointer is not.

One thing to watch: the GitHub pull-request-creation tooling may append its own
`_Generated by [Claude Code](https://claude.ai/code/session_...)_` footer
server-side, with the session ID substituted in, even when the body you wrote
did not contain one. When that happens, edit the description afterwards to
remove it — editing the body does not re-append it.
